using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public static class SaveData 
{
    //public static readonly string Save_Folder = Application.persistentDataPath + "/save/";
    //public static readonly string File_Exist = ".json";
    //public static void Save(string filename, string dataTosave)
    //{
    //    if (!Directory.Exists(Save_Folder))
    //    {
    //        Directory.CreateDirectory(Save_Folder);
    //    }

    //    File.WriteAllText(Save_Folder + filename + File_Exist, dataTosave);

        
    //}
    //public static string Load (string filename)
    //{
    //    string fileLoad = Save_Folder + filename + File_Exist;
    //    if (File.Exists(fileLoad))
    //    {
    //        string loaddata = File.ReadAllText(fileLoad);

    //        return loaddata;

    //    }
    //    else { return null; }
    //}
}
